
export const metadata = {
    title: "Recipe Search",
    description: "Search and view recipes",
};
export default function RecipeLayout({
    children,
}) {
    return (
        <div>
            {children}
        </div>
    );
}
